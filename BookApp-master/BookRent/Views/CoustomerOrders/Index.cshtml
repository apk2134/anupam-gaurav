@model IEnumerable<BookEntity.CoustomerOrder>

@{
    Layout = null;
}

@*<!DOCTYPE html>

    <html>
    <head>
        <meta name="viewport" content="width=device-width" />
        <title>Index</title>
    </head>
    <body>*@
<div class="col-lg-9 col-md-9 col-sm-9 col-xs-12 bhoechie-tab">
    <div class="control-group" id="AdminScreen">
        <div class="col-md- controls">
            <table class="table">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.OrderId)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.CoustomerId)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.OrderDate)
                    </th>
                    <th></th>
                </tr>

                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.OrderId)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.CoustomerId)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.OrderDate)
                        </td>
                        <td>
                            @*@Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })*@
                            @Html.ActionLink("OrdersController", "Index", new { id = item.OrderId })
                        </td>
                    </tr>
                }

            </table>
         </div>
    </div>
 </div>
            @*</body>
            </html>*@
